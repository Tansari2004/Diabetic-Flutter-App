import 'package:flutter/material.dart';

class MedicationTrackingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Medication Tracking'),
      ),
      body: Center(child: Text('Medication Tracking Page')),
    );
  }
}
