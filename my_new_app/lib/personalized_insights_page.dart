import 'package:flutter/material.dart';

class PersonalizedInsightsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Personalized Insights'),
      ),
      body: Center(
        child: Text('This is the Personalized Insights Page'),
      ),
    );
  }
}
