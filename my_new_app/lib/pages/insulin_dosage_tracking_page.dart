import 'package:flutter/material.dart';

class InsulinDosageTrackingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Insulin Dosage Tracking'),
      ),
      body: Center(child: Text('Insulin Dosage Tracking Page')),
    );
  }
}
